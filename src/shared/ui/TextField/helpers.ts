import { IconPropSize } from "shared/ui/icons/icon/Icon.interface";
import { TextFieldPropSize } from "./TextField.interface";

export const sizeMap: Record<TextFieldPropSize, IconPropSize> = {
  xs: "xs",
  s: "s",
  m: "s",
  l: "m",
};
